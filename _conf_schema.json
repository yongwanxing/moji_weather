{
    "api_base_url": {
        "description": "天气接口地址（默认指向墨迹天气公共接口，可按需修改）",
        "type": "string",
        "default": "https://api.yuafeng.cn/API/ly/moji.php"
    },
    "default_city": {
        "description": "默认查询城市（当指令不带城市时优先使用）",
        "type": "string",
        "default": "北京"
    },
    "forecast_days": {
        "description": "天气预报天数（1-7 天）",
        "type": "int",
        "default": 3,
        "hint": "控制 /天气 命令展示未来几天的预报"
    },
    "show_live_index": {
        "description": "是否展示生活指数",
        "type": "bool",
        "default": true
    }
}
